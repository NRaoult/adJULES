#set up paths 
adjules_location <- '../../' # experiment to be run in 'adjules_location'/temp/temp_experimentname/

code    <- paste0(adjules_location,'/code/')
result  <- paste0(adjules_location,'/results/')
